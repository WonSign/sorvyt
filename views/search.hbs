<head>
    <title>
        {{keyword}} - sotvyt
    </title>
</head>

{{#>layout}}
    {{#*inline "content"}}
        {{#each channelInfo}}
            <a href="/result?channelId={{channelId}}"
                <img src="{{channelProfile}}">
                <h3>{{channelName}}</h3>
            </a>
        {{/each}}
    {{/inline}}
{{/layout}}